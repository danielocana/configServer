server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/danielocanaheredia/spring-cloud-config-files
          search-paths:
            - 'station*'
          #username: username
          #password: pass